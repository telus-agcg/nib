#!/bin/bash

source "/usr/local/bin/_common"

COMMAND=$1

service=$(normalize $2)

shift
shift

service_options="$(echo $(command_json) | jq -r '.service_options')"

docker-compose run \
  --rm \
  $service_options \
  $service \
  $COMMAND $@
